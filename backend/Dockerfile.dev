FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Create uploads directory
RUN mkdir -p uploads

# Expose port
EXPOSE 5000

# Run in development mode with nodemon
CMD ["npm", "run", "dev"]
